# PAM stack for the Kavis lock screen (item 70).
#
# The system's own rules and nothing else: common-auth is what login and
# every other service use, so an account that cannot log in cannot
# unlock either, and a fingerprint or smartcard module added to
# common-auth works here without this file changing.
#
# The nopasswdlogin line first, and for the same reason lightdm has it:
# on a session where the system does not ask for a password — the live
# image puts its user in that group — the lock screen must not demand
# one either. (The live user really has no password since 0031-kavis-dirs
# deletes the "live" one live-config sets, so common-auth's pam_unix
# nullok would accept an empty answer anyway; this line is what keeps
# the stack honest on a system that follows the lightdm convention.)
#
# The lock calls pam_authenticate AND pam_acct_mgmt, so common-account
# below is consulted — it was included but never asked until the
# 11 Sep 2026 debug round.
#
# common-account is included as well: an expired or disabled account
# must not be able to unlock a screen it could not log into.
#
# No common-session and no common-password: unlocking starts no session
# and changes no password.
auth    sufficient      pam_succeed_if.so user ingroup nopasswdlogin
@include common-auth
@include common-account
