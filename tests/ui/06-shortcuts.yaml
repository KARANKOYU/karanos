# GENERATED by tools/gen-keybind-scenario.py — do not edit.
# Source: iso/config/hooks/normal/0210-openbox-keybinds.hook.chroot
#
# Every Kavis shortcut, pressed in a REAL session. Xvfb cannot
# prove a shortcut works (the snap bug passed there twice); the
# steps without an assertion still catch crashes, freezes and a
# panel that dies, through the runner's anomaly checks.
name: 06-shortcuts
title: Keyboard shortcuts
item: 6
allowed: [nemo, kavis-settings, kavis-taskmanager]
steps:
  - do: launch nemo --no-desktop /users/karan
    expect: window nemo visible
    timeout: 30000
    note: a window to aim the window shortcuts at
  - do: key super+Left
    expect: geometry nemo left-half
    timeout: 6000
    note: W-Left — snap to the left half
  - do: key super+Right
    expect: geometry nemo right-half
    timeout: 6000
    note: W-Right — snap to the right half
  - do: key super+Up
    expect: geometry nemo maximized
    timeout: 6000
    note: W-Up — maximize
  - do: key super+Down
    expect: geometry nemo on-screen
    timeout: 6000
    note: W-Down — restore
  - do: key super+Tab
    expect: popup kavis-panel visible
    timeout: 6000
    note: W-Tab — overview
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key super+z
    expect: popup kavis-panel visible
    timeout: 6000
    note: W-z — snap menu
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key super+v
    expect: popup kavis-panel visible
    timeout: 6000
    note: W-v — clipboard history
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key super+period
    expect: popup kavis-panel visible
    timeout: 6000
    note: W-period — emoji picker
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key XF86AudioRaiseVolume
    expect: ok
    note: XF86AudioRaiseVolume
  - do: key XF86AudioLowerVolume
    expect: ok
    note: XF86AudioLowerVolume
  - do: key XF86AudioMute
    expect: ok
    note: XF86AudioMute
  - do: key XF86MonBrightnessUp
    expect: ok
    note: XF86MonBrightnessUp
  - do: key XF86MonBrightnessDown
    expect: ok
    note: XF86MonBrightnessDown
  - do: key XF86AudioPlay
    expect: ok
    note: XF86AudioPlay
  - do: key XF86AudioNext
    expect: ok
    note: XF86AudioNext
  - do: key XF86AudioPrev
    expect: ok
    note: XF86AudioPrev
  - do: key ctrl+shift+Escape
    expect: window kavis-taskmanager visible
    timeout: 6000
    note: C-S-Escape — Task Manager
  - do: key XF86Launch5
    expect: popup kavis-panel visible
    timeout: 6000
    note: XF86Launch5 — start menu (Super via xcape)
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key ctrl+Escape
    expect: popup kavis-panel visible
    timeout: 6000
    note: C-Escape — start menu
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key super+r
    expect: popup kavis-panel visible
    timeout: 6000
    note: W-r — start menu with search
  - do: key Escape
    expect: popup kavis-panel hidden
    timeout: 4000
  - do: key super+e
    expect: window nemo visible
    timeout: 6000
    note: W-e — Files
  - do: key super+i
    expect: window kavis-settings visible
    timeout: 6000
    note: W-i — Settings
  - do: close window nemo
    expect: window nemo absent
    timeout: 8000
