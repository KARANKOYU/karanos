# Kavis — xdg-user-dirs-update disabled (item 0)
#
# The standard folders are created under /users/karan with English
# lower-case names (0031-kavis-dirs). With enabled=True,
# xdg-user-dirs-update would rewrite them in the local language on the
# first session (Turkish folder names) and the paths the components
# expect would change.
enabled=False
