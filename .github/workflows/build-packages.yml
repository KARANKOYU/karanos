name: Paketleri derle

# packages/ altındaki kavis-* .deb'lerini derler ve yapıt olarak yükler.
# ISO iş akışı bu iş akışını `uses:` ile çağırıp yapıtı doğrudan
# live-build'in config/packages.chroot/ dizinine koyuyor — böylece ISO'ya
# giren .deb ile burada test edilen .deb aynı koşunun ürünü oluyor.
#
# 13. aşamada bunların yerini aptly ile üretilen gerçek APT deposu alacak.

# TETİKLEYİCİLER (madde 22): push tetiklemesi kaldırıldı — kota. Paketler
# zaten her ISO derlemesinde bu iş akışı üzerinden derlenip doğrulanıyor;
# tek başına denemek için Actions sekmesinden elle çalıştırılabilir.
# Push'ta koşan tek şey hızlı kontroller (lint.yml).
on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    name: .deb üret
    runs-on: ubuntu-latest
    timeout-minutes: 30
    container:
      image: debian:trixie
    steps:
      - name: Derleme bağımlılıkları
        run: |
          set -eux
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential debhelper dpkg-dev fakeroot \
            librsvg2-bin x11-apps python3 ca-certificates git \
            ffmpeg fonts-dejavu-core gettext locales \
            valac libgtk-3-dev libwnck-3-dev libx11-dev \
            libgstreamer1.0-dev
          # gettext: rules'taki msgfmt + check-i18n (Grup D işi c).
          # locales/en_US: xx ekran görüntüsü adımı gerçek bir locale
          # ister (C altında gettext devre dışı — Codespace'te yaşandı).
          echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
          locale-gen

      - uses: actions/checkout@v7

      - name: Derle
        run: |
          set -eux
          chmod +x tools/build-packages.sh
          tools/build-packages.sh

      # Paketin içine gerçekten girmesi gerekenler girdi mi? Boş bir imleç
      # dizini ya da eksik duvar kağıdı derlemeyi durdurmaz, sessizce
      # eksik bir tema üretir — burada yakalanır.
      - name: Paket içeriği doğrulaması
        run: |
          set -eu
          deb=out/packages/kavis-theme_*_all.deb
          liste=$(dpkg-deb -c $deb)
          hata=0
          gerek() {
            adet=$(printf '%s\n' "$liste" | grep -c -- "$1" || true)
            if [ "$adet" -lt "$2" ]; then
              echo "::error::kavis-theme içinde '$1' $2 bekleniyordu, $adet var"
              hata=1
            else
              echo "✓ $1 — $adet"
            fi
          }
          gerek "usr/share/themes/Kavis/gtk-3.0/gtk.css"          1
          gerek "usr/share/themes/Kavis/openbox-3/themerc"        1
          gerek "usr/share/icons/Kavis-Cursors/cursors/left_ptr"  1
          gerek "usr/share/icons/Kavis-Cursors/cursors/"          80
          gerek "usr/share/icons/Kavis/48x48/apps/kavis.png"    1
          gerek "usr/share/backgrounds/kavis/kavis.png"         1
          gerek "usr/share/kavis/logo/koyu-k-logo.svg"          1
          gerek "usr/share/kavis/logo/acik-k-logo.svg"          1
          gerek "usr/share/kavis/logo/grub-arkaplan.png"        1
          gerek "usr/lib/os-release"                              1

          deb=out/packages/kavis-boot_*_all.deb
          liste=$(dpkg-deb -c $deb)
          gerek "usr/share/plymouth/themes/kavis/kavis.script"     1
          gerek "usr/share/plymouth/themes/kavis/logo.png"           1
          gerek "usr/share/plymouth/themes/kavis/urun-adi.png"       1
          gerek "usr/share/plymouth/themes/kavis/acilis-ipuclari.png" 1
          gerek "usr/share/plymouth/themes/kavis/made-by-karan.png"  1
          gerek "usr/share/kavis/boot/boot-sound.wav"                1
          gerek "etc/kavis/boot.conf"                                1
          gerek "lib/systemd/system/kavis-boot-sound.service"        1
          gerek "plymouth-quit.service.d/kavis.conf"                 1

          # Muzik gercekten cozulup fade uygulanmis mi? Bozuk bir WAV
          # acilista sessiz kalir ve sebebi ISO acilmadan anlasilmaz.
          rm -rf kb && mkdir kb && dpkg-deb -x $deb kb
          sure=$(ffprobe -v error -show_entries format=duration \
            -of default=noprint_wrappers=1:nokey=1 \
            kb/usr/share/kavis/boot/boot-sound.wav 2>/dev/null || echo 0)
          echo "acilis muzigi: ${sure}s"
          if awk -v s="$sure" 'BEGIN{exit !(s > 1 && s < 30)}'; then
            echo "✓ muzik suresi makul"
          else
            echo "::error::boot-sound.wav suresi ${sure}s — cozulme basarisiz olmus olabilir"
            hata=1
          fi

          # Ornek orani da denetlenir (boot-sound isi, Grup D): rules
          # 44100'e cevirir; farkli bir deger donusum adiminin
          # degistigini/atlandigini gosterir.
          oran=$(ffprobe -v error -select_streams a:0 \
            -show_entries stream=sample_rate \
            -of default=noprint_wrappers=1:nokey=1 \
            kb/usr/share/kavis/boot/boot-sound.wav 2>/dev/null || echo 0)
          echo "ornek orani: ${oran} Hz"
          if [ "$oran" = "44100" ]; then
            echo "✓ ornek orani 44100"
          else
            echo "::error::boot-sound.wav ornek orani ${oran} — beklenen 44100"
            hata=1
          fi

          exit $hata

      # Panel artık derlenmiş Vala ikilisi (madde 3): derleme hataları
      # zaten .deb aşamasında yakalanıyor. Metin tablosunun yerini
      # gettext aldı (Grup D işi c): denetim po/ üzerinde —
      # tools/check-i18n.sh (pot tazeliği, tr.po eksiksizliği,
      # msgfmt --check, xx.po senkronu).
      - name: kavis-panel ikilisi ve çeviriler
        run: |
          set -eu
          # check-i18n → gen-pot `git ls-files` kullanıyor (kopya
          # dosyaları dışarıda bırakmanın tek güvenilir yolu). Konteyner
          # root koşuyor, depo başka kullanıcıya ait → git "dubious
          # ownership" ile reddediyor (v0.3-test7 kırmızısının sebebi).
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          rm -rf kp && mkdir kp
          dpkg-deb -x out/packages/kavis-panel_*.deb kp
          test -x kp/usr/bin/kavis-panel
          test -f kp/usr/share/applications/kavis-panel.desktop
          test -f kp/usr/share/locale/tr/LC_MESSAGES/kavis.mo
          test -f kp/usr/share/locale/xx/LC_MESSAGES/kavis.mo
          tools/check-i18n.sh
          echo "kavis-panel ikilisi calisiyor, ceviriler tutarli"

      # Sahte uzun dil (xx) ekran görüntüsü: çevrilmemiş metin düz
      # İngilizce, taşan yerleşim kırpılmış ⟪…⟫ olarak GÖRÜNÜR. Yapıt
      # olarak yüklenir; elle inceleme için (otomatik piksel iddiası
      # yok — yerleşim değişimleri testi kırmasın).
      - name: xx sahte dil panel ekran görüntüsü
        run: |
          set -eu
          # Konteyner debian:trixie ve root — sudo yok/gerekmez.
          # test8 I4 dersi: `dpkg -i || apt -f install` paketi SÖKEREK
          # de "düzeltebiliyordu". test10 dersi: dpkg -i bağımlılık da
          # ÇÖZMEZ — panelin Depends'i büyüyünce (kavis-theme,
          # librsvg2-common, alsa-utils) adım kırıldı. Yerel .deb'ler
          # apt'a verilir: bağımlılıkları arşivden kendisi çözer,
          # çözemezse adım KIRMIZI olur, sökme riski yok.
          export DEBIAN_FRONTEND=noninteractive
          # x11-utils = xprop (KEYBIND adımı da bunlara güvenir; hepsi
          # burada, ISO paket listesine GİRMEZ — yalnız CI aracı).
          apt-get install -y -qq --no-install-recommends \
            xvfb openbox dbus dbus-x11 x11-xserver-utils x11-utils \
            xdotool xterm > /dev/null
          apt-get install -y -qq --no-install-recommends \
            ./out/packages/kavis-theme_*.deb \
            ./out/packages/kavis-panel_*.deb > /dev/null
          export DISPLAY=:99
          Xvfb :99 -screen 0 1280x800x24 &
          sleep 1
          openbox & sleep 1
          xsetroot -solid '#3a6b8a'
          # 4A: önce EN koşusu — Start ETİKETİ genişliğini stderr'e
          # yazar (start-genislik=NN); xx koşusuyla karşılaştırılır.
          dbus-run-session -- sh -c '
            LANG=en_US.UTF-8 LANGUAGE=en kavis-panel 2> panel-en.log &
            sleep 4
            pgrep -x kavis-panel > /dev/null || {
              echo "::error::kavis-panel (EN) ayakta degil"
              cat panel-en.log; exit 1
            }
          '
          pkill -x kavis-panel || true
          sleep 1
          dbus-run-session -- sh -c '
            LANG=en_US.UTF-8 LANGUAGE=xx kavis-panel 2> panel-xx.log &
            sleep 4
            pgrep -x kavis-panel > /dev/null || {
              echo "::error::kavis-panel ayakta degil"; cat panel-xx.log
              exit 1
            }
            ffmpeg -loglevel quiet -f x11grab -video_size 1280x800 \
              -i :99 -frames:v 1 -y panel-xx.ppm
          '
          # 4A denetimi: xx sahte dili metni ~2 kat uzatır — düğme EN'e
          # göre en az %30 büyümediyse metin bir yerde kırpılıyor
          # demektir. Yerleşim ayarı testi kırmasın: WARN, FAIL değil.
          en_w=$(grep -o 'start-genislik=[0-9]*' panel-en.log \
                 | tail -1 | cut -d= -f2)
          xx_w=$(grep -o 'start-genislik=[0-9]*' panel-xx.log \
                 | tail -1 | cut -d= -f2)
          xx_cut=$(grep -o 'start-kirpik=[0-9]' panel-xx.log | tail -1 | cut -d= -f2)
          echo "Start etiket genisligi: EN=${en_w:-?}px xx=${xx_w:-?}px kirpik=${xx_cut:-?}"
          # J: '··' xx dolgusudur, tasma degil — olcu Pango'nun gercek
          # '…' kirpmasi (start-kirpik). Genislik yalniz bilgi.
          if [ -z "${xx_cut:-}" ]; then
            echo "::warning::I18N-WIDTH-WARN: start-kirpik logu eksik"
          elif [ "$xx_cut" = "1" ]; then
            echo "::warning::I18N-WIDTH-WARN: xx Start etiketi '…' ile kirpildi (${xx_w}px, EN ${en_w}px)"
          fi
          python3 tools/screen-not-blank.py panel-xx.ppm || {
            echo "::error::panel-xx karesi bos/simsiyah"
            cat panel-xx.log || true
            exit 1
          }
          ffmpeg -loglevel quiet -i panel-xx.ppm -y panel-xx.png
          test -s panel-xx.png
      # C4 (v0.4-test1): her Kavis kısayolu gerçek rc.xml + 0210 hook'u
      # ile Xvfb'de basılır; beklenen eylem 2 sn içinde olmadıysa
      # KEYBIND-FAIL ve adım kırmızı. Tablo job summary'de.
      - name: KEYBIND — kısayol denetimi
        run: |
          set -eu
          # Araçlar bir önceki adımda kuruldu (xvfb, openbox, xdotool,
          # x11-utils, xterm); betik eksik olanı tek satırda söyler.
          DISPLAY_NO=77 tools/check-keybinds.sh /etc/xdg/openbox/rc.xml

      # Debug turu (3 Eyl): snap iki kez "Xvfb'de çalışıyor" sanılıp
      # gerçek sürüklemede çalışmadı. Bu adım pencereyi xdotool ile
      # taşımaz, başlığa basıp fareyi sürükler — taşımayı openbox yapar.
      - name: SNAP — kenara yapıştırma denetimi
        run: DISPLAY_NO=78 tools/check-snap.sh

      - name: xx ekran görüntüsünü yükle
        uses: actions/upload-artifact@v7
        with:
          name: panel-xx-ekran
          path: panel-xx.png

      - name: Boyut özeti
        run: |
          set -eu
          {
            echo "### Paketler"
            echo ""
            echo "| Paket | Boyut |"
            echo "|---|---|"
            for f in out/packages/*.deb; do
              echo "| \`$(basename "$f")\` | $(du -h "$f" | cut -f1) |"
            done
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@v7
        with:
          name: kavis-packages
          path: out/packages/*.deb
          retention-days: 14
